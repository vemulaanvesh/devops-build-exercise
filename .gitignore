# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment
.env
.env.*
!.env.example

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Terraform
*.tfstate
*.tfstate.*
.terraform/
crash.log
# Allow .terraform.lock.hcl: it pins providers and SHOULD be committed.
!.terraform.lock.hcl
# Don't commit real tfvars (use *.example as the committed template).
*.tfvars
!*.tfvars.example
# Plan artifacts
tfplan
plan.txt

# Local data
*.db
*.sqlite

# ─── Local-only candidate prep (never push to the take-home repo) ─────────
# Internal design rationale, generated images, and interview cheat-sheet.
# These live in the working copy for reference, but are not part of the
# submission to the interviewer.
docs/
assets/
INTERVIEW_PREP.md
path/to/

